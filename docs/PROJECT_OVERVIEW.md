# NodeFlow – Hybrid Edge/Cloud AI Graph Orchestrator

## Visión
NodeFlow permite diseñar, ejecutar y depurar grafos de conocimiento donde cada nodo puede ser un agente IA, una llamada a modelo local o un servicio externo. La interacción es *voz‑first* y la ejecución se reparte entre el cliente (Web) y el servidor (Edge/Cloud).