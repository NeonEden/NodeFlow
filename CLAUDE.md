# CLAUDE.md

El contrato de este repositorio está en **`AGENTS.md`** (raíz). Leelo antes de tocar nada: tiene los
comandos canónicos (los tres árbitros), las reglas de la casa, qué no tocar, el protocolo de entrega
(worktree → rama `agente/<tema>` → PR, nunca merge) y las trampas ya pagadas.

Este archivo existe sólo porque Claude Code busca `CLAUDE.md`: el contenido vive en `AGENTS.md` para
no tener dos contratos que se contradigan.
